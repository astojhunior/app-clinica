# Configuración de Apache para PHP-FPM (si se necesita en el futuro)
# Por ahora usamos mod_php incluido en php:8.2-apache

# Asegurar que el módulo de PHP está cargado
<IfModule mod_php.c>
    AddType application/x-httpd-php .php
    AddType application/x-httpd-php-source .phps
</IfModule>
